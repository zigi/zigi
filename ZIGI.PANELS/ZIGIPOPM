)Attr
 $ type(output) caps(off)
 @ type(text  ) caps(off) hilite(uscore) intens(low)
)Body Window(70,9)
+
+You have elected to exit the merge conflict resolution without
+resolving the conflicts. This leaves the conflict records in the OMVS
+repository file(s).@These records will now have to manually be+
@removed and the conflict resolved before proceeding.+
+
%Or+use the GITCMD dialog to issue the command:%git merge --abort
+
                   +Press%Enter+or%F3+to continue
)Init
 &zwinttl = 'zIGI Status Popup'
)Proc
)end
