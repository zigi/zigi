)attr default(%+_)
  _ type( input) intens(low ) just(left ) hilite(uscore)
  ~ type(text) intens(high) caps(off) just(asis ) hilite(reverse) color(turq)
  [ type(output) intens(high) caps(off) just(left) pas(on) skip(on)
)Body Expand(\\)
%Tutorial -\-\~The z ISPF Git Interface (zigi)%\-\- Tutorial
%Selection ===>_ZCMD                                                          +
+
                                %Config+
+
%Config+displays, and allows changing your user name and e-mail address which
+are used by%Git+when commits are made.
+
+Both are required, and the user name can be anything, as can the e-mail.
+However, it may be something that your organization requires to be accurate.
+
+Also required is the number of generations to be enabled for the Partitioned
+Datasets. Anything from 0 to the system limit (default=0).
+
+The Point-and-Shoot defaults control what happens if a double-click or Enter
+occurs outside the table or on a table row.%P+will display the Options popup
+if outside the table or the Row Options popup if within the table.%S+will
+do nothing if outside the table or default to select if within the table.
+
             +"A good beginning makes a good end" - Louis L'Amour
+
         +Press the%Enter+key to continue your zigi tutorial journey.
)Init
 .cursor  = &zcmd
)PROC
 &ZCONT = ZIGIH141
)END
/* -------------------------------------------------------- */
/* zigi - the z/OS ISPF Git Interface                       */
/* Copyright (C) 2020 GPL V3 - Henri Kuiper and Lionel Dyck */
/* -------------------------------------------------------- */
