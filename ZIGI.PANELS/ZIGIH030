)attr default(%+_)
  _ type( input) intens(low ) just(left ) hilite(uscore)
  ~ type(text) intens(high) caps(off) just(asis ) hilite(reverse) color(turq)
  [ type(output) intens(high) caps(off) just(left) pas(on)
)Body Expand(\\)
%Tutorial -\-\~The z ISPF Git Interface (zigi)%\-\- Tutorial
%Selection ===>_ZCMD                                                          +
+
                             %Special Characters+
+
+Special characters in z/OS datasets and member names will be converted
+when moved to the repository.
+
+This is done via the -M option for the cp command.
+
+In the%status+log you will see the 'OMVS'-names (as this is from the
+OMVS-side of things.
+
+
+
+
+
     "The best competition I have is against myself to become better."
         - John Wooden
+
+
+
           +Press the%Enter+key to begin your zigi tutorial journey.
)Init
 .cursor = &zcmd
)PROC
 &ZUP = ZIGIH020
)END
