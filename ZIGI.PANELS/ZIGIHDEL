)Attr
 $ type(output) caps(off)
 @ type(input ) hilite(uscore) intens(low)
 + type(text) skip(on) intens(low)
)Body Window(55,12)
+
+Deleting a repository from Git will remove it from Git
+management but will%not+delete the OMVS files.
+
+Deleting the OMVS files will delete the OMVS directory
+and%all+OMVS files in the repository.
+
+Deleting the z/OS datasets will delete%all+the z/OS
+datasets used in the repository.
+
+
                +Press%F3+to continue.
)Init
 &zwinttl = 'zigi Delete Confirmation Tutorial'
)Proc
)end
