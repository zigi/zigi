)attr default(%+_)
  _ type( input) intens(low ) just(left ) hilite(uscore)
  ~ type(text) intens(high) caps(off) just(asis ) hilite(reverse) color(turq)
  [ type(output) intens(high) caps(off) just(left) pas(on)
)Body Expand(\\)
%Tutorial -\-\~The z ISPF Git Interface (zigi)%\-\- Tutorial
%Selection ===>_ZCMD                                                          +
+
                                    %Menu+
+
%Menu+ will hide the command and row selection menus to provide more space
+on the screen for usable data. Use command%Menu+to toggle the menu back or
+use command%O+to display a popup menu from which to select.
+
+
+
+
+
+
+
+
+
            +"Reputation is for time; character is for eternity."
            + -- J. B. Gough
+
+
           +Press the%Enter+key to begin your zigi tutorial journey.
)Init
 .cursor = &zcmd
)PROC
 &ZCONT = ZIGIH315
)END
