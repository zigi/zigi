)attr default(%+_)
  _ type( input) intens(low ) just(left ) hilite(uscore)
  ~ type(text) intens(high) caps(off) just(asis ) hilite(reverse) color(turq)
  [ type(output) intens(high) caps(off) just(left) pas(on) skip(on)
)Body Expand(\\)
%Tutorial -\-\~The z ISPF Git Interface (zigi)%\-\- Tutorial
%Selection ===>_ZCMD        \ \ %Scroll ===>_zcsr
+
                                %AddDsn+
+
+Using%SA+Selective Add to add members in binary format first adds all
+members as text members, then a list of all members is presented to allow
+the Selection%S+of members as binary.
+
+The members selected as binary will be copied in binary format to the
+OMVS filesystem, they will be entered into the .gitattributes file with a
+binary designation, and they will be tagged in OMVS as binary files.
+
+
+
+
+  "When your work speaks for itself, don't interrupt."
+                  - Henry J. Kaiser
+
+

         +Press the%Enter+key to continue your zigi tutorial journey.
)Init
 .cursor  = &zcmd
 &zscr = CSR
)PROC
 &zcont = zigih214
)END
