)Attr
 $ type(output) caps(off)
 # type(output) hilite(reverse) color(turq) caps(off)
)Body Expand(\\)
+zigihelp \-\#zigtitle   +-\-\ zigihelp
%Command ===>_zcmd
+
+The main panel displays a lists with the available Local Git Repositories with
+the following options:
+
      %Commands:
      %Clone        +Clone a Remote Repository (creates z/OS datasets)
      %Create       +Create an empty Local Repository
      %GitHelp+     +Display git Help information
      %SSH          +View the public part of your SHH-keypair
      +
      %Line Selections:
      %S            +Select a Local Repository to work in
      %V            +View the Local Repository
      %D            +Delete a Local Repository
+                    - Repository in OMVS and zIGI removed
+                    - z/OS dataset remain
+
+
+
\ \Press%Enter+to continue or%F3+to return to the dialog\ \
)Init
)Proc
 &zcont = zigihp1
)end
