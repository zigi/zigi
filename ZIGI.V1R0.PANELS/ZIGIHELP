)Attr Default(%+_)
  _ type( input) intens(low ) caps(off) just(left ) hilite(uscore)
  $ type(output) intens(low ) caps(off) just(left )
  + type(text) intens(low) skip(on)
  % type(text) intens(high) skip(on)
  # type(output) hilite(reverse) color(turq) caps(off)
)Body  Expand(\\)
+zigihelp \-\#zigtitle   +-\-\ zigihelp
%Command ===>_zcmd
%
%Commands:
            %Branch   +Change to a new, or existing, Branch
            %Copy     +Copy all updates to USS and do Git Add and Commit
            %GLog     +View Git Log
            %PULl     +Pull updates from the Remote Repository
            %PUsh     +Push updates to Remote Repository
            %SHow     +View Git Status
            %STatus   +Git Show
%
%Selections:
            %B+Browse +Browse the Data Set
            %E+Edit   +Edit the Data Set
            %V+View   +View the Data Set
%
)Init
)Proc
 &zup = zigihr0
 &zcont = zigihr1
)End
